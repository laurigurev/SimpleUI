struct sui_context;
struct sui_backend;
struct sui_layout;
struct sui_style;
struct sui_color;
enum sui_alignment { SUI_ALIGN_LEFT, SUI_ALIGN_RIGHT, SUI_ALIGN_MIDDLE };

void sui_begin(struct sui_context* sui);
void sui_end(struct sui_context* sui);
void sui_row(struct sui_context* sui);
void sui_align_columns(struct sui_context* sui);
void sui_style(struct sui_context* sui, struct sui_style* style);
i32 sui_button(struct sui_context* sui, const char* name);
i32 sui_checkbox(struct sui_context* sui, const char* name);
void sui_slider(struct sui_context* sui, const char* name, f32* value);

struct sui_color { u8 r; u8 g; u8 b; u8 a; };
struct sui_layout {
	f32 x;
	f32 y;
	f32 w;
	f32 h;
};
struct sui_context {
	...
	
	i32 hot_item;
	i32 active_item;

	...
}
